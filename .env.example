# =============================================================================
# CyberVantage Environment Configuration Example
# =============================================================================
# Copy this file to .env and fill in your actual values

# -----------------------------------------------------------------------------
# Flask Configuration
# -----------------------------------------------------------------------------
FLASK_ENV=development
FLASK_HOST=127.0.0.1
FLASK_PORT=5000
SECRET_KEY=your-secret-key-here

# -----------------------------------------------------------------------------
# Database Configuration
# -----------------------------------------------------------------------------
# For SQLite (local development):
# DATABASE_URL=sqlite:///users.db

# For Azure PostgreSQL (recommended for production):
# DATABASE_URL=postgresql://username:password@servername.postgres.database.azure.com:5432/dbname?sslmode=require
# Or use Vercel-style variables:
# POSTGRES_URL=postgres://username:password@servername.postgres.database.azure.com:5432/dbname
# POSTGRES_PRISMA_URL=postgres://username:password@servername-pooler.postgres.database.azure.com:5432/dbname?pgbouncer=true
# POSTGRES_URL_NON_POOLING=postgres://username:password@servername.postgres.database.azure.com:5432/dbname

# Default (for local testing):
DATABASE_URL=sqlite:///users.db

# -----------------------------------------------------------------------------
# AI Provider Configuration - Multi-AI with Fallback
# -----------------------------------------------------------------------------

# Primary AI Model - Choose which AI provider to use first
# Options: "azure" or "gemini"
# Default: "azure"
PRIMARY_AI_MODEL=azure

# Azure OpenAI Configuration
# Required if PRIMARY_AI_MODEL=azure or as fallback when PRIMARY_AI_MODEL=gemini
AZURE_OPENAI_KEY=your-azure-openai-api-key
AZURE_OPENAI_ENDPOINT=https://your-resource.openai.azure.com/openai/deployments/your-deployment/chat/completions?api-version=2023-05-15
AZURE_OPENAI_DEPLOYMENT=gpt-4.1-nano

# Google Gemini Configuration  
# Required if PRIMARY_AI_MODEL=gemini or as fallback when PRIMARY_AI_MODEL=azure
GOOGLE_API_KEY=your-google-gemini-api-key

# -----------------------------------------------------------------------------
# How AI Fallback Works
# -----------------------------------------------------------------------------
# 1. If PRIMARY_AI_MODEL=azure:
#    - First tries Azure OpenAI
#    - If Azure fails, automatically falls back to Google Gemini
#
# 2. If PRIMARY_AI_MODEL=gemini:
#    - First tries Google Gemini
#    - If Gemini fails, automatically falls back to Azure OpenAI
#
# Both providers should be configured for full fallback support.
# The system will work with just one provider but without fallback capability.
# -----------------------------------------------------------------------------

# -----------------------------------------------------------------------------
# Security Configuration (Optional)
# -----------------------------------------------------------------------------
# Add any API keys for threat intelligence, VirusTotal, etc.
