# CyberVantage Security Configuration
# IMPORTANT: Set all these values for production deployment

# === SECURITY SECRETS (REQUIRED for Production) ===
# Generate secure random values for production
FLASK_SECRET=your-secure-random-secret-here-32-chars-min
JWT_SECRET=your-jwt-secret-here-32-chars-min
CSRF_SECRET=your-csrf-secret-here-32-chars-min
ENCRYPTION_KEY=your-fernet-encryption-key-here

# === API CONFIGURATIONS ===
# VirusTotal API Configuration
# Get your API key from https://www.virustotal.com/gui/my-apikey
VIRUSTOTAL_API_KEY=your_virustotal_api_key_here

# Google API Configuration (Optional for AI features)
GOOGLE_API_KEY=your_google_api_key_here

# Azure OpenAI Configuration (Optional for AI features)  
AZURE_OPENAI_API_KEY=your_azure_openai_key_here
AZURE_OPENAI_ENDPOINT=https://your-resource.openai.azure.com/
AZURE_OPENAI_API_VERSION=2024-02-15-preview

# === DATABASE CONFIGURATION ===
DATABASE_URL=sqlite:///instance/app.db

# === APPLICATION CONFIGURATION ===
# Set to 'production' for production, 'development' for development
FLASK_ENV=development

# === SECURITY NOTES ===
# 1. Use 'openssl rand -base64 32' to generate secure secrets
# 2. Use 'python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"' for ENCRYPTION_KEY
# 3. Never commit real secrets to version control
# 4. Set FLASK_ENV=production for production deployments